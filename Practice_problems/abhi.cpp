#include<bits/stdc++.h>
using namespace std;



int main(){

    long long arr[] = {1,2,3,4};

    int size_of_Array = sizeof(arr);
    cout<<size_of_Array<<endl;
    int size_of_element = sizeof(arr[0]);
    cout<<size_of_element<<endl;

    int no_of_elements = sizeof(arr)/sizeof(arr[0]);
    cout<<no_of_elements<<endl;

}